# ============================================
# Touwaka Mate - 环境变量配置示例
# 复制此文件为 .env 并填入实际值
# ============================================

# 专家ID（必填）
# 指定当前副本代表哪个专家
EXPERT_ID=eric

# 数据库配置（必填）
# 这些环境变量会被 config/database.json 中的占位符引用
DB_HOST=localhost
DB_PORT=3306
DB_NAME=touwaka_mate
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_CONNECTION_LIMIT=10

# ============================================
# API 服务器配置（api-server.js）
# ============================================

# API 服务器端口（可选，默认 3000）
API_PORT=3000

# 前端 URL（用于 CORS）
FRONTEND_URL=http://localhost:5173

# JWT 密钥配置（必填，生产环境请使用强密钥）
# 生成随机密钥: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-jwt-secret-key-change-in-production-min-32-chars
JWT_REFRESH_SECRET=your-jwt-refresh-secret-key-change-in-production

# ============================================
# 专家实例配置（index.js）
# ============================================

# 运行模式（可选，默认 cli）
# cli: 命令行交互模式
# server: HTTP API 服务器模式
RUN_MODE=cli

# 实例服务器端口（可选，默认 3000）
PORT=3000

# 用户ID（CLI 模式时使用）
USER_ID=cli_user

# 技能目录路径（可选，文件系统模式时使用）
SKILLS_BASE_PATH=/shared/skills

# ============================================
# 日志配置
# ============================================

# 日志级别（可选，默认 info）
# debug, info, warn, error
LOG_LEVEL=info

# ============================================
# LLM API 密钥
# ============================================

# OpenAI API Key
OPENAI_API_KEY=sk-your-openai-api-key

# DeepSeek API Key
DEEPSEEK_API_KEY=sk-your-deepseek-api-key

# 其他 Provider API Keys
# ANTHROPIC_API_KEY=sk-your-anthropic-api-key
