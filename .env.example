# ============================================
# Chat Expert Instance - 环境变量配置示例
# 复制此文件为 .env 并填入实际值
# ============================================


# 数据库配置（必填）
# 也可以直接配置在 config/database.json 中
DB_HOST=localhost
DB_PORT=3306
DB_NAME=touwaka_mate
DB_USER=your_db_user
DB_PASSWORD=your_db_password


# API 服务器端口（可选，默认 3000）
PORT=3000

# 技能目录路径（可选，文件系统模式时使用）
# 如果使用 Docker 共享卷，设置为容器内的共享路径
SKILLS_BASE_PATH=/data/skills

# 日志级别（可选，默认 info）
# debug, info, warn, error
LOG_LEVEL=info

# 消息平台配置（如果使用消息平台功能）
# MESSAGE_PLATFORM=websocket
# MESSAGE_PLATFORM_URL=ws://localhost:8080
