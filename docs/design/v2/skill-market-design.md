# 技能市场设计方案

## 概述

技能管理系统包含两部分：
1. **本地技能库**：`/shared/skills/` 目录 + `skills` 表注册
2. **技能市场Modal**：从远程仓库搜索、下载和安装技能

```
┌─────────────────────────────────────────────────────────────┐
│                    技能管理系统架构                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  本地技能库 (Local Skills)                           │   │
│  │  ┌─────────────────┐  ┌─────────────────┐          │   │
│  │  │ /shared/skills/ │  │   skills 表     │           │   │
│  │  │  ├─ web-search/ │  │  - 技能注册信息  │           │   │
│  │  │  ├─ code-exec/  │  │  - 启用状态      │           │   │
│  │  │  └─ ...         │  │  - 安装来源URL   │           │   │
│  │  └─────────────────┘  └─────────────────┘          │   │
│  └─────────────────────────────────────────────────────┘   │
│                           ▲                                 │
│                           │ 安装                            │
│                           │                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  技能市场 Modal (Skill Market)                       │   │
│  │  ┌─────────────────────────────────────────────┐    │   │
│  │  │  搜索来源：                                   │    │   │
│  │  │  1. GitHub: besoeasy/open-skills (默认)     │    │   │
│  │  │  2. 自定义URL (ClawHub等)                    │    │   │
│  │  └─────────────────────────────────────────────┘    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 一、本地技能管理

### 1.1 技能与工具的关系

```
┌─────────────────────────────────────────────────────────────┐
│  技能 (Skill)                                                │
│  - 一个功能模块/包                                           │
│  - 包含一组相关的工具                                        │
│  - 类似 npm 包的概念                                         │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  web-search 技能                                     │   │
│  │  ├── search 工具 (shell)     # 搜索网页              │   │
│  │  └── fetch 工具 (nodejs)     # 获取URL内容           │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  data-tools 技能                                     │   │
│  │  ├── csv-preview 工具 (shell) # 预览CSV              │   │
│  │  ├── analyze 工具 (python)    # 分析数据             │   │
│  │  └── visualize 工具 (nodejs)  # 生成图表             │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘

类比：
- 技能 ≈ npm 包 / Python 模块
- 工具 ≈ 包里的函数 / 方法
```

### 1.2 文件系统结构

```
/shared/skills/
├── web-search/
│   ├── skill.md           # 技能说明（必需）
│   ├── index.js           # 技能实现（必需）
│   └── config.json        # 配置模板（可选）
├── code-executor/
│   ├── skill.md
│   ├── index.js
│   └── config.json
└── ...
```

### 1.2 数据库表

```sql
-- skills 表（技能元数据）
CREATE TABLE skills (
  id VARCHAR(32) PRIMARY KEY,
  name VARCHAR(128) NOT NULL,
  description TEXT,
  source_type ENUM('filesystem', 'database') DEFAULT 'filesystem',
  source_path VARCHAR(256),          -- 文件系统路径
  source_url VARCHAR(512),           -- 安装来源URL
  skill_md TEXT,                     -- skill.md 内容
  config TEXT,                       -- JSON配置
  version VARCHAR(32),
  author VARCHAR(128),
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- skill_tools 表（工具定义，运行时在工具级别）
CREATE TABLE skill_tools (
  id INT AUTO_INCREMENT PRIMARY KEY,
  skill_id VARCHAR(32) NOT NULL,
  name VARCHAR(64) NOT NULL,
  description TEXT,
  
  -- 执行方式
  kind ENUM('shell', 'nodejs', 'python', 'builtin') DEFAULT 'shell',
  command VARCHAR(512),              -- shell命令或脚本路径
  args JSON,                         -- 参数模板
  
  FOREIGN KEY (skill_id) REFERENCES skills(id) ON DELETE CASCADE,
  UNIQUE KEY (skill_id, name)
);

-- expert_skills 关联表（已有）
-- 管理专家启用的技能及其配置
```

### 1.3 工具执行类型（kind）

参考ZeroClaw的设计，每个工具独立定义执行方式：

| kind | 执行方式 | 示例 |
|------|----------|------|
| `shell` | Shell命令执行 | `ddgr --json "{{query}}"` |
| `nodejs` | Node.js脚本 | `bun run index.js` |
| `python` | Python脚本 | `python3 main.py` |
| `builtin` | 系统内置实现 | 无需外部脚本 |

### 1.4 运行时配置

```javascript
// config/skills.js
module.exports = {
  // 全局运行时配置
  runtimes: {
    shell: {
      path: '/bin/bash',
      args: ['-c'],
    },
    nodejs: {
      // 优先使用 bun（更快），降级到 node
      path: process.env.NODE_RUNTIME || 'bun',
      args: process.env.NODE_RUNTIME === 'node' ? [] : ['run'],
    },
    python: {
      path: process.env.PYTHON_RUNTIME || 'python3',
      args: [],
      venvPath: process.env.PYTHON_VENV || null,
    },
  },
  
  // 技能存储路径
  skillsBasePath: process.env.SKILLS_PATH || '/shared/skills',
}
```

### 1.5 技能目录结构

一个技能可以包含多种脚本：

```
/shared/skills/
├── web-search/
│   ├── skill.md             # 技能说明
│   ├── search.sh            # Shell脚本（ddgr封装）
│   └── fetch.js             # Node.js脚本（URL获取）
│
├── data-analysis/
│   ├── skill.md
│   ├── analyze.py           # Python脚本
│   └── visualize.js         # Node.js脚本
│
└── system-tools/
    └── skill.md             # 纯声明式，使用builtin工具
```

### 1.6 本地技能API

```
GET    /api/skills           # 获取本地技能列表
GET    /api/skills/:id       # 获取技能详情
POST   /api/skills           # 创建/注册技能
PUT    /api/skills/:id       # 更新技能
DELETE /api/skills/:id       # 删除技能
POST   /api/skills/scan      # 扫描本地目录，自动注册新技能
```

---

## 二、技能市场 Modal

### 2.1 技能来源

| 来源 | URL | 说明 |
|------|-----|------|
| **默认市场** | `github:besoeasy/open-skills` | ZeroClaw社区技能库 |
| **自定义URL** | 用户输入 | 任意GitHub仓库或ClawHub等平台 |

### 2.2 市场API

```
# 获取远程技能列表
GET /api/skills/market
Query:
  - source: 'community' | 'url'
  - url: string (当source=url时必填)
  - search: string
  - page: number
  - size: number

Response:
{
  "items": [
    {
      "id": "web-search",
      "name": "Web Search",
      "description": "Search the web",
      "author": "community",
      "version": "1.0.0",
      "tags": ["web", "search"],
      "installed": false,
      "repository_url": "https://github.com/besoeasy/open-skills/tree/main/skills/web-search"
    }
  ],
  "total": 50,
  "page": 1,
  "pages": 5
}

# 从市场安装技能
POST /api/skills/install
Body:
{
  "skill_id": "web-search",
  "source": "community"           # 或 "url"
}

# 从自定义URL安装
POST /api/skills/install-from-url
Body:
{
  "url": "https://github.com/user/skill-repo",
  "skill_name": "my-skill"        # 可选，不指定则安装全部
}
```

### 2.3 安装流程

```
用户点击"安装"
    │
    ▼
后端下载技能文件
    │
    ├─► GET https://raw.githubusercontent.com/{owner}/{repo}/main/skills/{skill_id}/skill.md
    ├─► GET https://raw.githubusercontent.com/{owner}/{repo}/main/skills/{skill_id}/index.js
    └─► GET https://raw.githubusercontent.com/{owner}/{repo}/main/skills/{skill_id}/config.json
    │
    ▼
保存到 /shared/skills/{skill_id}/
    │
    ▼
注册到 skills 表
    │
    ▼
返回安装结果
```

---

## 三、技能格式规范

### 3.1 兼容ZeroClaw格式

支持 `besoeasy/open-skills` 仓库的技能格式：

```
skills/
├── web-search/
│   ├── SKILL.toml    # ZeroClaw格式（优先）
│   ├── SKILL.md      # 或Markdown格式
│   └── index.js
```

**SKILL.toml 示例（参考ZeroClaw格式）：**
```toml
[skill]
name = "web-search"
description = "Search the web using DuckDuckGo"
version = "1.0.0"
author = "community"
tags = ["web", "search"]

# 工具定义 - 每个工具独立定义执行方式
[[tools]]
name = "search"
description = "Perform a web search"
kind = "shell"                    # shell | nodejs | python | builtin
command = "ddgr --json -n 5"      # shell命令
args = { query = "{{input}}" }    # 参数模板

[[tools]]
name = "fetch"
description = "Fetch URL content"
kind = "nodejs"
command = "fetch.js"              # 相对于技能目录的脚本路径
args = { url = "{{input}}" }
```

**混合运行时技能示例：**
```toml
[skill]
name = "data-tools"
description = "Data analysis and visualization"
version = "1.0.0"

# Shell工具
[[tools]]
name = "csv-preview"
description = "Preview CSV file"
kind = "shell"
command = "head -n 20 {{file}}"

# Python工具
[[tools]]
name = "analyze"
description = "Analyze data with pandas"
kind = "python"
command = "analyze.py"
args = { file = "{{input}}", output = "json" }

# Node.js工具
[[tools]]
name = "visualize"
description = "Generate chart"
kind = "nodejs"
command = "chart.js"
args = { data = "{{input}}" }

# 内置工具（无需脚本）
[[tools]]
name = "format"
description = "Format number"
kind = "builtin"
# builtin工具由系统内置实现，无需command
```

**纯Shell技能示例（兼容open-skills）：**
```toml
[skill]
name = "system-utils"
description = "System utility commands"
version = "1.0.0"

[[tools]]
name = "disk-usage"
description = "Check disk usage"
kind = "shell"
command = "df -h"

[[tools]]
name = "process-list"
description = "List running processes"
kind = "shell"
command = "ps aux | head -n 20"
```

**skill.md 示例：**
```markdown
# web-search

网页搜索技能。

## 工具

### search
执行网页搜索。
- `query` (string, required): 搜索关键词
```

### 3.2 解析优先级

1. 优先解析 `SKILL.toml`（ZeroClaw格式）
2. 降级解析 `SKILL.md` 或 `skill.md`
3. 从 `index.js` 提取工具定义

---

## 四、前端组件

### 4.1 SkillMarketModal.vue

```
┌─────────────────────────────────────────────────────────────┐
│  📦 技能市场                                           [×]  │
├─────────────────────────────────────────────────────────────┤
│  🔍 搜索技能...                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📦 web-search                                    v1.0│   │
│  │    使用DuckDuckGo搜索网页                           │   │
│  │    👤 community  ⭐ 128                            │   │
│  │    [web] [search]                                  │   │
│  │                              [安装] [查看源码]      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 📦 code-executor                                  v2.1│   │
│  │    安全执行代码片段                                 │   │
│  │    👤 zeroclaw   ⭐ 89                             │   │
│  │    [code] [execute]                                │   │
│  │                              [已安装] [查看源码]    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  [从URL安装...]                          [刷新]  [关闭]     │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 从URL安装对话框

```
┌─────────────────────────────────────┐
│  从URL安装技能                      │
├─────────────────────────────────────┤
│  GitHub仓库或技能平台URL:           │
│  ┌───────────────────────────────┐  │
│  │ https://github.com/user/skill │  │
│  └───────────────────────────────┘  │
│  提示: 支持GitHub仓库或ClawHub链接  │
├─────────────────────────────────────┤
│              [取消]  [安装]         │
└─────────────────────────────────────┘
```

---

## 五、实现计划

### Phase 1 - MVP

| 任务 | 说明 |
|------|------|
| 后端：本地技能API | CRUD + 扫描注册 |
| 后端：市场API | 从open-skills获取列表 |
| 后端：安装API | 下载文件 + 注册数据库 |
| 前端：SkillMarketModal | 搜索、列表、安装 |
| 前端：从URL安装 | 支持自定义URL |

### Phase 2 - 增强（暂不实现）

- 版本检查和更新
- 技能评分统计
- 本地缓存优化

### Phase 3 - 高级（暂不实现）

- 私有仓库支持
- 技能依赖管理
- 自动更新

---

## 六、配置

```javascript
// config/skills.js
module.exports = {
  // 技能存储路径
  skillsBasePath: process.env.SKILLS_PATH || '/shared/skills',
  
  // 运行时配置
  runtimes: {
    nodejs: {
      // 优先使用 bun（更快），降级到 node
      path: process.env.NODE_RUNTIME || 'bun',
      args: process.env.NODE_RUNTIME === 'node' ? [] : ['run'],
      // 示例：bun run index.js  或  node index.js
    },
    python: {
      path: process.env.PYTHON_RUNTIME || 'python3',
      args: [],
      // 虚拟环境支持（可选）
      venvPath: process.env.PYTHON_VENV || null,
    },
    bash: {
      path: '/bin/bash',
      args: [],
    },
  },
  
  // 默认技能市场
  defaultRegistry: {
    type: 'github',
    owner: 'besoeasy',
    repo: 'open-skills',
  },
  
  // GitHub API Token（可选，提高速率限制）
  githubToken: process.env.GITHUB_TOKEN,
}
```

### 环境变量

```bash
# .env
SKILLS_PATH=/shared/skills

# Node.js 运行时（可选：bun, node, /path/to/node）
NODE_RUNTIME=bun

# Python 运行时（可选：python3, python, /path/to/python）
PYTHON_RUNTIME=python3
PYTHON_VENV=/shared/skills/.venv

# GitHub API Token（提高速率限制）
GITHUB_TOKEN=ghp_xxxx
```

---

## 七、安全考虑

1. **代码沙箱**：技能在子进程中执行，有资源限制
2. **用户确认**：安装前显示技能来源和权限要求
3. **来源追溯**：`source_url` 字段记录安装来源

---

*文档版本: v1.1*
*创建日期: 2026-02-22*
